#include "main.h"

/**
 * main - check if function prints whether integer is positive or negative
 *
 * Return: success always (0)
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
